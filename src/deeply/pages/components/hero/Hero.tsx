import './Hero.scss';

const Hero = () => {
    return (
        <section className="hero">
            <div className="hero__container">
                deep linking made easy
            </div>
        </section>
    );
}; 

export default Hero;